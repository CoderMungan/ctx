//   /    ctx:                         https://ctx.ist
// ,'`./    do you remember?
// `.,'\
//   \    Copyright 2026-present Context contributors.
//                 SPDX-License-Identifier: Apache-2.0

package mcp

const (
	// ToolStatus is the MCP tool name for context status.
	ToolStatus = "ctx_status"
	// ToolAdd is the MCP tool name for adding entries.
	ToolAdd = "ctx_add"
	// ToolComplete is the MCP tool name for completing tasks.
	ToolComplete = "ctx_complete"
	// ToolDrift is the MCP tool name for drift detection.
	ToolDrift = "ctx_drift"
	// ToolRecall is the MCP tool name for querying session history.
	ToolRecall = "ctx_recall"
	// ToolWatchUpdate is the MCP tool name for structured context updates.
	ToolWatchUpdate = "ctx_watch_update"
	// ToolCompact is the MCP tool name for compacting tasks.
	ToolCompact = "ctx_compact"
	// ToolNext is the MCP tool name for suggesting the next task.
	ToolNext = "ctx_next"
	// ToolCheckTaskCompletion is the MCP tool name for task completion nudge.
	ToolCheckTaskCompletion = "ctx_check_task_completion"
	// ToolSessionEvent is the MCP tool name for session lifecycle events.
	ToolSessionEvent = "ctx_session_event"
	// ToolRemind is the MCP tool name for listing reminders.
	ToolRemind = "ctx_remind"
)