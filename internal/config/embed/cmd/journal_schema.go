//   /    ctx:                         https://ctx.ist
// ,'`./    do you remember?
// `.,'\\
//   \    Copyright 2026-present Context contributors.
//                 SPDX-License-Identifier: Apache-2.0

package cmd

// Use strings for journal schema subcommands.
const (
	// UseJournalSchema is the cobra Use string for the journal schema command.
	UseJournalSchema = "schema"
	// UseJournalSchemaCheck is the cobra Use string for the
	// journal schema check command.
	UseJournalSchemaCheck = "check"
	// UseJournalSchemaDump is the cobra Use string for the
	// journal schema dump command.
	UseJournalSchemaDump = "dump"
)

// DescKeys for journal schema subcommands.
const (
	// DescKeyJournalSchema is the description key for the
	// journal schema command.
	DescKeyJournalSchema = "journal.schema"
	// DescKeyJournalSchemaCheck is the description key for the
	// journal schema check command.
	DescKeyJournalSchemaCheck = "journal.schema.check"
	// DescKeyJournalSchemaDump is the description key for the
	// journal schema dump command.
	DescKeyJournalSchemaDump = "journal.schema.dump"
)
