//   /    ctx:                         https://ctx.ist
// ,'`./    do you remember?
// `.,'\\
//   \    Copyright 2026-present Context contributors.
//                 SPDX-License-Identifier: Apache-2.0

package flag

// DescKeys for journal schema flags.
const (
	// DescKeyJournalSchemaCheckDir is the description key for the
	// journal schema check --dir flag.
	DescKeyJournalSchemaCheckDir = "journal.schema.check.dir"
	// DescKeyJournalSchemaCheckAllProjects is the description key for the
	// journal schema check --all-projects flag.
	DescKeyJournalSchemaCheckAllProjects = "journal.schema.check.all-projects"
	// DescKeyJournalSchemaCheckQuiet is the description key for the
	// journal schema check --quiet flag.
	DescKeyJournalSchemaCheckQuiet = "journal.schema.check.quiet"
)
