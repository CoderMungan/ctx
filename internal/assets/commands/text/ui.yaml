# General UI text strings for ctx CLI.
# Used by assets.TextDesc() for prompts, labels, and status output.

group.getting-started:
  short: "Getting Started:"
group.context:
  short: "Context (source of truth):"
group.artifacts:
  short: "Artifacts (.context/ files):"
group.sessions:
  short: "Sessions:"
group.runtime:
  short: "Runtime:"
group.integration:
  short: "Integration:"
group.diagnostics:
  short: "Diagnostics:"
group.utilities:
  short: "Utilities:"

agent.instruction:
  short: 'Before starting work, confirm to the user: "I have read the required context files and I''m following project conventions."'
agent.packet-meta:
  short: 'Generated: %s | Budget: %d tokens | Used: ~%d tokens'
agent.packet-title:
  short: '# Context Packet'
agent.section-constitution:
  short: '## Constitution (NEVER VIOLATE)'
agent.section-conventions:
  short: '## Key Conventions'
agent.section-decisions:
  short: '## Recent Decisions'
agent.section-learnings:
  short: '## Key Learnings'
agent.section-read-order:
  short: '## Read These Files'
agent.section-summaries:
  short: '## Also Noted'
agent.section-tasks:
  short: '## Current Tasks'
changes.fallback-label:
  short: 24 hour(s) ago (default)
changes.code-authors:
  short: '- **Authors**: %s'
changes.code-commits:
  short: '- **%s** since reference point'
changes.code-dirs:
  short: '- **Directories touched**: %s'
changes.code-heading:
  short: '### Code Changes'
changes.code-latest:
  short: '- **Latest**: %s'
changes.ctx-heading:
  short: '### Context File Changes'
changes.ctx-line:
  short: '- `%s` - modified %s'
changes.heading:
  short: '## Changes Since Last Session'
changes.hook-commits:
  short: '%s since last session'
changes.hook-commits-extra:
  short: ' (latest: %s)'
changes.hook-ctx-files:
  short: 'Context files changed (%s): %s'
changes.hook-prefix:
  short: 'Changes since last session: '
changes.none:
  short: No changes detected since reference point.
changes.ref-point:
  short: '**Reference point**: %s'
changes.since-prefix:
  short: 'since '
compact.clean:
  short: '✓ Nothing to compact - context is already clean'
compact.heading:
  short: Compact Analysis
compact.sections-removed:
  short: '✓ Removed %d empty sections from %s'
compact.separator:
  short: '================'
compact.summary:
  short: '✓ Compacted %d items'
compact.task-error:
  short: '⚠ Error processing TASKS.md: %v'
config.already-on:
  short: 'already on %s profile'
config.created:
  short: 'created %s from %s profile'
config.switched:
  short: 'switched to %s profile'
confirm.proceed:
  short: 'Proceed? [y/N] '
drift.cleared:
  short: ✓ Index cleared (no %s found)
drift.dead-path:
  short: references path that does not exist
drift.entry-count:
  short: 'has %d entries (recommended: ≤%d)'
drift.missing-file:
  short: required context file is missing
drift.missing-package:
  short: package %s is not documented
drift.regenerated:
  short: ✓ Index regenerated with %d entries
drift.secret:
  short: may contain secrets (constitution violation)
drift.stale-age:
  short: last modified %d days ago
drift.staleness:
  short: has many completed items (consider archiving)
drift.applying:
  short: Applying fixes...
drift.fixed-count:
  short: '✓ Fixed %d issue(s)'
drift.skipped-count:
  short: '○ Skipped %d issue(s) (cannot auto-fix)'
drift.fix-error:
  short: '⚠ Error: %s'
drift.rechecking:
  short: Re-checking after fixes...
drift.fix-staleness:
  short: '✓ Fixed staleness in %s (archived completed tasks)'
drift.fix-staleness-err:
  short: 'staleness: %v'
drift.fix-missing:
  short: '✓ Created missing file: %s'
drift.fix-missing-err:
  short: 'missing %s: %v'
drift.skip-dead-path:
  short: '○ Cannot auto-fix dead path in %s:%d (%s)'
drift.skip-stale-age:
  short: '○ Cannot auto-fix file age: %s'
drift.skip-sensitive-file:
  short: '○ Cannot auto-fix potential sensitive file: %s'
drift.archived:
  short: '  Archived %d completed tasks to %s'
drift.report-heading:
  short: Drift Detection Report
drift.report-separator:
  short: '======================'
drift.violations-heading:
  short: '❌ VIOLATIONS (%d)'
drift.violation-line:
  short: '  - %s: %s'
drift.violation-line-loc:
  short: '  - %s:%d %s'
drift.violation-rule:
  short: ' (rule: %s)'
drift.warnings-heading:
  short: '⚠️  WARNINGS (%d)'
drift.path-refs-label:
  short: '  Path References:'
drift.path-ref-line:
  short: "  - %s:%d references '%s' (not found)"
drift.staleness-label:
  short: '  Staleness:'
drift.staleness-line:
  short: '  - %s %s'
drift.other-label:
  short: '  Other:'
drift.other-line:
  short: '  - %s: %s'
drift.passed-heading:
  short: '✅ PASSED (%d)'
drift.passed-line:
  short: '  - %s'
drift.status-violation:
  short: 'Status: VIOLATION - constitution violations detected'
drift.status-warning:
  short: 'Status: WARNING - issues detected that should be addressed'
drift.status-ok:
  short: 'Status: OK - no drift detected'
drift.check-path-refs:
  short: Path references are valid
drift.check-staleness:
  short: No staleness indicators
drift.check-constitution:
  short: Constitution rules respected
drift.check-required:
  short: All required files present
drift.check-file-age:
  short: No stale files by age
drift.stale-header:
  short: 'comment header in %s does not match template — run ctx init --force to sync'
drift.check-template-header:
  short: All context file headers match templates
guide.default:
  short: |
    ctx - persistent AI context

    GETTING STARTED
      ctx init              Create .context/ directory with templates
      ctx status            Show context health summary
      ctx doctor            Diagnose configuration issues

    TRACKING DECISIONS & KNOWLEDGE
      ctx add -t TYPE       Add a decision, learning, convention, or task
      ctx complete          Mark a task as done in TASKS.md
      ctx decision reindex  Rebuild the DECISIONS.md index table

    BROWSING HISTORY
      ctx recall list       List imported session transcripts
      ctx recall show ID    Read a specific session transcript
      ctx journal site      Generate a browsable journal site

    AI CONTEXT
      ctx agent             Load AI-optimized context packet (use --budget)
      ctx load              Output raw assembled context
      ctx drift             Detect stale or invalid context

    MAINTENANCE
      ctx compact           Archive completed tasks, trim context
      ctx sync              Reconcile codebase changes with docs
      ctx pad               Encrypted scratchpad for sensitive notes

    KEY SKILLS
      /ctx-commit           Commit with context capture
      /ctx-implement        Execute a plan step-by-step
      /ctx-next             Suggest what to work on next
      /ctx-reflect          Surface persist-worthy items
      /ctx-wrap-up          End-of-session persistence ceremony
      /ctx-drift            Detect and fix context drift
      /ctx-remember         Recall project context
      /ctx-recall           Browse session history

    RECIPES
      Start session:      ctx agent --budget 4000
      Record decision:    ctx add -t decision "Use PostgreSQL for persistence"
      Check health:       ctx status && ctx drift
      End session:        invoke /ctx-wrap-up

    Full listings: ctx guide --skills | ctx guide --commands
guide.commands-heading:
  short: 'CLI Commands:'
guide.command-line:
  short: '  %-14s %s'
init.label-entry-templates:
  short: Entry templates
init.label-scratchpad:
  short: Scratchpad
init.label-project-dirs:
  short: Project dirs
init.label-permissions:
  short: Permissions
init.label-plugin-enable:
  short: Plugin enablement
init.confirm-claude:
  short: Would you like to append ctx context management instructions?
events.empty:
  short: No events logged.
events.human-format:
  short: '%-19s  %-5s  %-24s  %s'
import.count-convention:
  short: '%d convention'
import.count-decision:
  short: '%d decision'
import.count-learning:
  short: '%d learning'
import.count-task:
  short: '%d task'
journal.consolidate-count:
  short: '(×%d)'
journal.project-label:
  short: ' (%s)'
journal.moc.browse-by:
  short: '## Browse by'
journal.moc.file-page-stats:
  short: '**%d sessions** touching this file.'
journal.moc.file-stats:
  short: '**%d files** across **%d sessions** - **%d popular**, **%d long-tail**'
journal.moc.files-description:
  short: '- sessions grouped by file touched'
journal.moc.nav-description:
  short: Navigation hub for all journal entries.
journal.moc.session-link:
  short: '- [%s](%s%s) (%d sessions)%s'
journal.moc.topic-page-stats:
  short: '**%d sessions** with this topic.'
journal.moc.topic-stats:
  short: '**%d topics** across **%d sessions** - **%d popular**, **%d long-tail**'
journal.moc.topics-description:
  short: '- sessions grouped by topic'
journal.moc.topics-label:
  short: '**Topics**: %s'
journal.moc.type-label:
  short: '**Type**: %s'
journal.moc.type-page-stats:
  short: '**%d sessions** of type *%s*.'
journal.moc.type-stats:
  short: '**%d types** across **%d sessions**'
journal.moc.types-description:
  short: '- sessions grouped by type'
journal.moc.browse-item:
  short: '- %s %s'
journal.moc.heading-topics:
  short: '# Topics'
journal.moc.heading-popular:
  short: '## Popular Topics'
journal.moc.heading-longtail:
  short: '## Long-tail Topics'
journal.moc.heading-files:
  short: '# Key Files'
journal.moc.heading-frequent:
  short: '## Frequently Touched'
journal.moc.heading-single:
  short: '## Single Session'
journal.moc.heading-types:
  short: '# Session Types'
journal.moc.heading-month:
  short: '## %s'
journal.moc.item-sessions:
  short: '- %s (%d sessions)'
journal.moc.item-named:
  short: '- **%s** - %s'
journal.moc.item-file-sessions:
  short: '- %s (%d sessions)'
journal.moc.item-file-named:
  short: '- `%s` - %s'
journal.moc.item-listed:
  short: '- %s'
journal.moc.page-title:
  short: '# %s'
journal.moc.code-title:
  short: '# `%s`'
journal.moc.topics-moc-link:
  short: Topics MOC
journal.moc.topic-separator:
  short: ' · '
memory.diff-new-format:
  short: |
    +++ %s (source)
memory.diff-old-format:
  short: |
    --- %s (mirror)
memory.import-review:
  short: Imported from MEMORY.md - review and update as needed
memory.import-source:
  short: auto-memory import
memory.publish-conventions:
  short: '## Key Conventions'
memory.publish-decisions:
  short: '## Recent Decisions'
memory.publish-learnings:
  short: '## Recent Learnings'
memory.publish-tasks:
  short: '## Pending Tasks'
memory.publish-title:
  short: |+
    # Project Context (managed by ctx)

pad.key-created:
  short: Scratchpad key created at %s
prune.dry-run-line:
  short: '  would prune: %s (age: %s)'
prune.dry-run-summary:
  short: Dry run - would prune %d files (skip %d recent, preserve %d global)
prune.error-line:
  short: '  error removing %s: %v'
prune.summary:
  short: Pruned %d files (skipped %d recent, preserved %d global)
rc.parse_warning:
  short: 'ctx: warning: failed to parse %s: %v (using defaults)'
stats.empty:
  short: No stats recorded yet.
stats.header-format:
  short: '%-19s  %-8s  %6s  %8s  %4s  %-12s'
stats.line-format:
  short: '%-19s  %-8s  %6d  %7s  %3d%%  %-12s'
stopwords:
  short: the and for that this with from are was were been have has had but not you all can her his she its our they will each make like use way may any into when which their about would there what also should after before than then them could more some other only just see add new update how
summary.active:
  short: '%d active'
summary.completed:
  short: '%d completed'
summary.decision:
  short: 1 decision
summary.decisions:
  short: '%d decisions'
summary.empty:
  short: empty
summary.invariants:
  short: '%d invariants'
summary.loaded:
  short: loaded
summary.term:
  short: 1 term
summary.terms:
  short: '%d terms'
recall.meta-summary:
  short: '%s · %s · %s'
recall.token-summary:
  short: '%s (in: %s, out: %s)'
recall.tool-count-line:
  short: '%s: %d'
site.feed-generated:
  short: 'Generated %s (%d entries)'
site.feed-skipped:
  short: 'Skipped:'
site.feed-warnings:
  short: 'Warnings:'
site.feed-item:
  short: '  %s'
site.skip-cannot-read:
  short: '%s - cannot read file'
site.skip-no-frontmatter:
  short: '%s - no frontmatter found'
site.skip-malformed:
  short: '%s - malformed frontmatter'
site.skip-parse-error:
  short: '%s - %s'
site.skip-not-finalized:
  short: '%s - not finalized'
site.skip-missing-title:
  short: '%s - missing title'
site.skip-missing-date:
  short: '%s - missing date'
site.warn-no-summary:
  short: '%s - no summary paragraph found'
sync.config.description:
  short: Found %s but %s not documented
sync.config.suggestion:
  short: Document %s in %s
sync.deps.description:
  short: Found %s (%s) but no dependency documentation
sync.deps.suggestion:
  short: Consider documenting key dependencies in %s or create %s
sync.dir.description:
  short: Directory '%s/' exists but not documented
sync.dir.suggestion:
  short: Add '%s/' to %s with description
task-archive.dry-run-block:
  short: |-
    Dry run - no files modified

    Would archive %d completed tasks (keeping %d pending)

    Archived content preview:
    %s
    %s%s
task-archive.no-completed:
  short: No completed tasks to archive.
task-archive.pending-remain:
  short: '  %d pending tasks remain in TASKS.md'
task-archive.skip-incomplete:
  short: No tasks to archive (%d skipped due to incomplete children).
task-archive.skipping:
  short: '! Skipping (has incomplete children): %s'
task-archive.success:
  short: ✓ Archived %d completed tasks to %s
task-archive.success-with-age:
  short: ✓ Archived %d tasks to %s (older than %d days)
task-snapshot.created-format:
  short: 'Created: %s'
task-snapshot.header-format:
  short: '# TASKS.md Snapshot - %s'
task-snapshot.saved:
  short: ✓ Snapshot saved to %s
time.ago:
  short: ' ago'
time.commit:
  short: commit
time.commit-count:
  short: 1 commit
time.commits:
  short: commits
time.commits-count:
  short: '%d commits'
time.day:
  short: day
time.day-count:
  short: 1 day
time.days:
  short: days
time.days-count:
  short: '%d days'
time.hour:
  short: hour
time.hour-count:
  short: 1 hour
time.hours:
  short: hours
time.hours-count:
  short: '%d hours'
time.just-now:
  short: just now
time.minute:
  short: minute
time.minute-count:
  short: 1 minute
time.minutes:
  short: minutes
time.minutes-count:
  short: '%d minutes'
watch.apply-failed:
  short: |
    ✗ Failed to apply [%s]: %v
watch.apply-success:
  short: |
    ✓ Applied: [%s] %s
watch.close-log-error:
  short: 'failed to close log file: %v'
watch.dry-run-banner:
  short: DRY RUN - no changes will be made
watch.dry-run-preview:
  short: |
    ○ Would apply: [%s] %s
watch.stop-hint:
  short: Press Ctrl+C to stop
watch.watching:
  short: Watching for context updates...
why.admonition-format:
  short: '> **%s**'
why.banner:
  short: |2-

       /    ctx:                         https://ctx.ist
     ,'`./    do you remember?
     `.,'\
       \
          {}  -> what
          ctx -> why
why.blockquote-prefix:
  short: '> '
why.bold-format:
  short: '**%s**'
why.menu-item-format:
  short: '  [%d] %s'
why.menu-prompt:
  short: "\nSelect a document (1-3): "
